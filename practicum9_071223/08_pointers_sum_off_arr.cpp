/*#include <iostream>

int sumOfArray(int* arr, int n);

int main()
{
	const int MAX_SIZE = 100;
	int arr[MAX_SIZE] = {};
	
	int n = 0;
	std::cin >> n;

	for (int i = 0; i < n; i++)
	{
		std::cin >> *(arr + i);
	}

	std::cout<<sumOfArray(arr, n);

	return 0;
}

int sumOfArray(int* arr, int n)
{
	int sum = 0;
	for (int i = 0; i < n; i++)
	{
		sum += *(arr + i);
	}

	return sum;
}
*/